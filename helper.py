from blockchain import Node, RingNode


def non_bootstrap_node(node: Node):
    """Initialization of a non bootstrap node

    Args:
        node (Node): The node to be initialized
    """
    pass

def bootstrap_node(node: Node):
    """Initialization of the bootstrap node

    Args:
        node (Node): The bootstrap node to be initialized
    """
    pass
